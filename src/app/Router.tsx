import { BrowserRouter } from "react-router-dom";
import Routes from "@app/Routes";

const Router = () => {
  return (
    <BrowserRouter>
      <Routes />
    </BrowserRouter>
  );
};
export default Router;
